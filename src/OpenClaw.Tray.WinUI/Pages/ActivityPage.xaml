<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with filter and clear -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="HeaderText" Text="Activity" Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" Grid.Column="0"/>
            <ComboBox x:Name="FilterCombo" Grid.Column="2" Margin="0,0,8,0" Width="140"
                      SelectionChanged="OnFilterChanged">
                <ComboBoxItem Content="All" Tag="all" IsSelected="True"/>
                <ComboBoxItem Content="Sessions" Tag="session"/>
                <ComboBoxItem Content="Usage" Tag="usage"/>
                <ComboBoxItem Content="Channels" Tag="channel"/>
                <ComboBoxItem Content="Nodes" Tag="node"/>
                <ComboBoxItem Content="Notifications" Tag="notification"/>
                <ComboBoxItem Content="System" Tag="system"/>
            </ComboBox>
            <Button Content="Clear All" Grid.Column="3" Click="OnClearAll"/>
        </Grid>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyText" Text="No activity items" Grid.Row="1"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="Collapsed"/>

        <!-- Activity list -->
        <ListView x:Name="ActivityList" Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                            Padding="12" Margin="0,0,0,6">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding TimeText}" Grid.Column="0" Width="60"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Top"/>
                            <Border Grid.Column="1" Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="4" Padding="6,2" VerticalAlignment="Top">
                                <TextBlock Text="{Binding Category}"
                                           Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"/>
                            </Border>
                            <StackPanel Grid.Column="2" Spacing="2">
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Details}" TextWrapping="Wrap"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Visibility="{Binding DetailsVisibility}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
