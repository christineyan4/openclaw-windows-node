<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.DiagnosticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="16">
            <TextBlock Text="Diagnostics" Style="{StaticResource SubtitleTextBlockStyle}"/>

            <!-- Health & Updates -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Run Health Check" Click="OnHealthCheck"/>
                        <Button Content="Check for Updates" Click="OnCheckUpdates"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Warnings -->
            <Border x:Name="WarningsCard" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Visibility="Collapsed">
                <StackPanel Spacing="8">
                    <TextBlock Text="Warnings" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <ListView x:Name="WarningsList" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Spacing="2" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Title}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Detail}" TextWrapping="Wrap"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Support Files -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Support Files" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Open Log File" Click="OnOpenLogFile"/>
                        <Button Content="Logs Folder" Click="OnOpenLogFolder"/>
                        <Button Content="Config Folder" Click="OnOpenConfigFolder"/>
                        <Button Content="Diagnostics Folder" Click="OnOpenDiagFolder"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Copy Diagnostics -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Copy Diagnostics" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Support Context" Click="OnCopySupportContext"/>
                        <Button Content="Debug Bundle" Click="OnCopyDebugBundle"/>
                        <Button Content="Browser Setup" Click="OnCopyBrowserSetup"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Port Diagnostics" Click="OnCopyPortDiag"/>
                        <Button Content="Capability Diagnostics" Click="OnCopyCapDiag"/>
                        <Button Content="Node Inventory" Click="OnCopyNodeInv"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Channel Summary" Click="OnCopyChannelSummary"/>
                        <Button Content="Activity Summary" Click="OnCopyActivitySummary"/>
                        <Button Content="Extensibility Summary" Click="OnCopyExtSummary"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Advanced -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Advanced" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Restart SSH Tunnel" Click="OnRestartSshTunnel"/>
                        <Button Content="Run Setup Wizard" Click="OnSetupWizard"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
