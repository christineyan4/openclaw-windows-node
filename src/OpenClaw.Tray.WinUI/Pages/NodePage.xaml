<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.NodePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="16">
            <!-- Header -->
            <TextBlock x:Name="HeaderText" Text="Node" Style="{StaticResource SubtitleTextBlockStyle}"/>

            <!-- Node mode indicator -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="4">
                    <TextBlock Text="Node Mode" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="NodeModeText" Text="Disabled"/>
                </StackPanel>
            </Border>

            <!-- Device ID -->
            <Border x:Name="DeviceIdCard" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="4" Grid.Column="0">
                        <TextBlock Text="Device ID" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="DeviceIdText" Text="" IsTextSelectionEnabled="True"
                                   Style="{StaticResource BodyTextBlockStyle}"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Copy" Click="OnCopyDeviceId" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Nodes list -->
            <TextBlock x:Name="EmptyText" Text="No nodes registered"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Visibility="Collapsed"/>

            <ListView x:Name="NodesList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                                Padding="16" Margin="0,0,0,8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <FontIcon Grid.Column="0" Glyph="{Binding StatusGlyph}" FontSize="20"
                                          Foreground="{Binding StatusColor}" VerticalAlignment="Top"/>
                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock Text="{Binding DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="{Binding ShortId}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                    <TextBlock Text="{Binding DetailText}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Actions -->
            <Button Content="Copy Node Summary" Click="OnCopyNodeSummary"/>
        </StackPanel>
    </ScrollViewer>
</Page>
